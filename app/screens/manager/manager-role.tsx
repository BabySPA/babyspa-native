import { Box } from 'native-base';
import { AppStackScreenProps } from '~/app/types';

export default function ManagerRole({
  navigation,
}: AppStackScreenProps<'ManagerRole'>) {
  return <Box flex={1}>ManagerRole</Box>;
}
